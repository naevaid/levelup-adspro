import { ModulePlaceholder } from "@/components/app/module-placeholder";

export default function SettingsPage() {
  return (
    <ModulePlaceholder
      eyebrow="Settings"
      title="Konfigurasi tenant dasar"
      description="Area settings disiapkan sebagai placeholder untuk organization profile, notification preference, dan pengaturan sensitif tenant."
      primaryCtaLabel="Kembali ke dashboard"
    />
  );
}
